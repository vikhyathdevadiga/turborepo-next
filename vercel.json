{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "env": {
    "ENABLE_EXPERIMENTAL_COREPACK": "1"
  },
  "ignoreCommand": "pnpm -v && exit 1",
  "buildCommand": "pnpm -v && cd ../../ && pnpm turbo build --filter=web..."
}
